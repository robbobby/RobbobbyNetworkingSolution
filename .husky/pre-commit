#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks..."

echo "🔨 Building solution..."
dotnet build

# Run unit tests
echo "🧪 Running unit tests..."
if out="$(dotnet test --filter "Category=Unit" --verbosity minimal 2>&1)"; then
    echo "$out"
    echo "✅ Unit tests completed successfully!"
else
    echo "$out"
    if printf '%s' "$out" | grep -qiE 'No test is available'; then
        echo "⚠️  No unit tests found - continuing with commit"
    else
        echo "⚠️  Unit tests failed - continuing (pre-commit is non-blocking)"
    fi
fi

echo "✅ Pre-commit checks completed successfully!"
